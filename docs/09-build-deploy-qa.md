# Track 9 — Build & Deploy

**Job:** Turn the synthesized narrative into a functional, deployable page and ship a live link.

## Stack
- Build with code (AI-assisted). **No no-code site builders** (Squarespace/Wix/Webflow disallowed).
- Decided (Track 8.5): single static page under `site/`, Tailwind + brand tokens, no framework. Self-hosted/Google fonts. Deploy to Vercel.

## Build order
1. Scaffold `site/` and wire up the design tokens from Track 8.
2. Lay out sections per the Track 5 outline (structure is already decided — don't improvise).
3. Drop in copy (Track 5) and proof (Track 7); place CTAs per Track 6.

## Deploy
- Ship a **live link** (Vercel). This is the deliverable's share method (brief: live link or project link). Record the URL here and in the write-up.
- Page is **desktop-optimized for now** (see assumption in `docs/00`). Functional + palatable to a desktop reviewer is the bar; mobile/a11y/perf/cross-browser polish are explicitly out of scope for this take-home.

## Output
A deployed landing page + the live URL.
